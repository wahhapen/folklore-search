{
  "name": "folklore-search",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "search": "node scripts/search-corpus.mjs",
    "benchmark": "node scripts/run-search-benchmark.mjs",
    "test": "vitest run",
    "validate": "npm run benchmark && npm test"
  },
  "devDependencies": {
    "vitest": "4.1.10"
  }
}
